[workspace]
resolver = "2"
members = [
    "crates/tablet-backend",
    "crates/tablet-shell",
]

[workspace.package]
edition = "2024"
license = "Apache-2.0"
version = "0.1.0"

[profile.release]
codegen-units = 8
lto = false
opt-level = "s"
panic = "abort"
strip = true
